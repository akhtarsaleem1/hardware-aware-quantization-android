# Android research application

Status: `NOT_IMPLEMENTED`

The prior plant-disease Flutter UI is not copied because it is tied to one FP16
classifier and aggregate-only benchmark output. The replacement must support
model/precision selection, requested and effective delegates, thread count,
raw per-inference export, device/thermal context, and a clear research-prototype
label before physical-device experiments begin.

